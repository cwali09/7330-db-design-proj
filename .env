# .env (Place in the project root directory - used by docker-compose)
# These should match the credentials your backend application expects
DB_NAME=social_media_db
DB_PASSWORD=root_secret
DB_ROOT_PASSWORD=root_secret # Only needed for docker setup, not the app connection

# You might also put the backend API port here if needed elsewhere
# API_PORT=3001 